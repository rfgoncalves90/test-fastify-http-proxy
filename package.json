{
    "scripts": {
        "compile": "tsc -p tsconfig.json",
        "build": "tsc -p tsconfig.json && esbuild dist\\applicationServer.js --bundle --platform=node --target=node22 --minify --outfile=bundled.js"
    },
    "dependencies": {
        "@fastify/http-proxy": "^11.0.1",
        "fastify": "^5.2.1"
    },
    "devDependencies": {
        "@types/node": "^22.12.0",
        "esbuild": "^0.24.2",
        "typescript": "^5.7.3"
    }
}